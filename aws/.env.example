# Copy to .env and fill in. NEVER commit .env (it's gitignored).

# Case-insensitive regex matched against every posting title on the Notion
# (Ashby) board — alerts when a matching posting appears. Catches postings
# that don't exist yet (e.g. future intern roles).
TITLE_PATTERN='(software|engineer|engineering).*\bintern(ship)?s?\b|\bintern(ship)?s?\b.*(software|engineer|engineering)'

# Optional: specific Notion (Ashby) posting URLs to watch by id, comma-separated
#JOB_URLS=https://jobs.ashbyhq.com/notion/297b4ece-765f-4eea-b1b8-46057cb6501f

# Spectrum Cloud credentials (Photon dashboard -> project Settings,
# https://app.photon.codes)
PROJECT_ID=
PROJECT_SECRET=

# Who gets the iMessage alerts — E.164 numbers, comma-separated.
# Each number must be registered as a project user on the Photon
# dashboard (shared-pool plan requirement).
RECIPIENTS=+15557654321,+15551112222,+15553334444,+15556667777

# Optional email alerts via SES. EMAIL_FROM must be a verified SES
# identity; sending to unverified recipients needs SES production
# access. Leave blank to disable.
EMAIL_FROM=
EMAIL_TO=
